{
  "name": "@variance-authority/example-dynamic-route-flake",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": { "test": "vitest run src/dynamic-route.chromium.test.ts" },
  "dependencies": {
    "@variance-authority/core": "workspace:^",
    "@variance-authority/dom": "workspace:^",
    "@variance-authority/playwright": "workspace:^",
    "@variance-authority/png": "workspace:^",
    "@variance-authority/react": "workspace:^",
    "react": "^19",
    "react-dom": "^19"
  },
  "devDependencies": { "esbuild": "^0.25.0", "playwright": "^1.49.0", "vitest": "^2.1.8" }
}
