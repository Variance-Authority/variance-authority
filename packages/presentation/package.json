{
  "name": "@variance-authority/presentation",
  "version": "0.0.0-beta.1",
  "description": "Sense the presentation relationships inside one live interface — spacing, alignment, prominence, repetition — as evidence an agent can read.",
  "keywords": [
    "visual-regression",
    "visual-testing",
    "variance-authority",
    "presentation",
    "layout",
    "accessibility",
    "agent",
    "playwright"
  ],
  "license": "MIT",
  "author": "Mechanic Garden",
  "homepage": "https://variance-authority.dev#packages",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Variance-Authority/variance-authority.git",
    "directory": "packages/presentation"
  },
  "bugs": "https://github.com/Variance-Authority/variance-authority/issues",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./playwright": {
      "types": "./dist/playwright.d.ts",
      "default": "./dist/playwright.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "skills",
    "mark.svg"
  ],
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@variance-authority/core": "workspace:^",
    "@variance-authority/dom": "workspace:^",
    "@variance-authority/react": "workspace:^",
    "@variance-authority/report": "workspace:^"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.49 <2"
  },
  "peerDependenciesMeta": {
    "@playwright/test": {
      "optional": true
    }
  }
}
