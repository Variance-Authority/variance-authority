# Specs

**Everything in this directory is unfinished. That is the entry criterion.**

A spec says what a capability must do, written before the code, and it lives
exactly as long as the capability is incomplete. The moment the thing ships, the
decisions it forced move into an ADR and **the spec is deleted**. So the answer
to "what is left to build" is `ls docs/specs/`, and it is not a table you have to
read a status column out of.

The alternative is a status column, and it fails in a specific way. A directory
of files in four states — `not built`, `built, not wired`, `built, never run`,
`built` — cannot be read from a filename or a number, so nobody can tell whether
`0006` is an idea or a shipped adapter without opening it, and the column drifts
out of date faster than the code it describes. **A directory that needs decoding
stops being read**, and the debt it exists to make visible becomes the thing
hiding it.

## What is left

| Capability | What exists | What does not |
|---|---|---|
| [History service and drift queries](0002-history-store.md) | The rows, the drift arithmetic, both backends, the wire, and a caller: `variance run` records its run, the hashes that moved, and every subject that failed to read the same way twice, then reports how often that has happened before ([ADR-0031](../context/adr/0031-the-run-asks-what-is-recorded-now.md), [ADR-0032](../context/adr/0032-a-flake-rate-divides-by-the-runs-that-asked.md)). | **The `reach` delta, and the journey run end to end.** Four of the five questions are asked by the run and answered in the report. Half of `reach` is answered — *where does this component appear*, for one commit, by the suite comparing itself against itself ([`composition.md`](../composition.md)) — and what is left is the word *started*: a delta needs two of those graphs and nothing writes one to the record. The spec's headline case, a button gaining 2px eleven times, is produced by the pipeline and has never been *run*: that needs eleven runs and eleven approvals, not more code. |
| [Locale runs](0008-locale-runs.md) | `compareLocales` — untranslated strings, boxes that stopped fitting, and what went uncompared — measured against real Chromium layout. | **The axis.** Nothing reads a `locales` key; the word does not appear in the CLI's config. A locale comparison still means hand-writing a test. |
| [Order dependence in a run](0012-order-dependence-in-a-run.md) | `collectAlone`, a budgeted second pass, and a change that vanishes in a clean world reported as order dependence rather than as a regression — refused by `accept`, labelled in the summary, led with in `variance_describe`. Items 0, 1 and 3 of six. | **The sharpener and the memory.** No probe runs, so the writer is never named — which is a position, not a gap, since module-scope leaks are invisible to any probe. What is missing is `packages/session` wired in for the stylesheet case, history so a leak can be seen to persist or to be fixed, and a tool of its own instead of riding on two. |
| [Storage and cache primitives](0011-storage-and-cache-primitives.md) | Applicability pruning, component hashes, nine inspection rules, three baseline backends, both placements — `flat` for a corpus and `beside` for baselines that belong to code in the same tree — a settlement path that reads no baseline image when nothing moved, and a `RenderCache` whose failures cost a render rather than the run. | **Three seams.** The layout is a closed union of two words, so a third placement is a pull request rather than a config key; the rule set is a closed union, so a lens cannot be added; nothing reduces a render document before hashing it. Items 0 and 1 of five are discharged, 2 is half-discharged, and the rest are in the file, ordered. Where the record lives once the image is placed in the tree is [0040](0040-a-baseline-in-git-is-a-picture.md), whose first step — a `records` root of its own — has landed. |
| [Hosted: who the caller is, and what the bill counts](0014-hosted-who-the-caller-is-and-what-the-bill-counts.md) | Two capability tokens in `tribunal` with a constant-time comparison and one indistinguishable refusal; a history service that will not start without a token; a remote baseline store that takes a bearer. | **An identity, a meter, and a deployable artifact.** A token resolves to a capability and never to *who* or *whose*: the schema is already project-keyed on every table, but the project arrives from a deployment setting and from `?project=`, so nothing stops a credential naming another tenant's. An approval names nobody. Nothing counts anything, and a counter would be a number the customer cannot check. `serveRenderer` has no authentication at all. |
| [A real agent](0013-a-real-agent.md) | Eight MCP tools as pure functions over a report, a protocol codec, `variance serve` — tested against text. `variance adjudicate --claims` and `variance_adjudicate` read a finished run back against a declaration, all three arms, over-claiming visible. The docket-shaped `adjudicate` in `core` still has no caller. | **The author loop.** An agent editing components has no way to hold an observation, observe again, and be answered with the difference — the one setting where two documents, and so the strongest answer, are structurally guaranteed. The intent wire answers over a completed run, not a held one. Missing: the session boundary, `observe` and `claim`, and the recorded sessions that would correct the tool list. |
| [CI that has run](0016-ci-that-has-run.md) | The workflow, the composite action, `variance comment` and its marker, the commit-back with its three refusals, and a Bitbucket recipe. | **One execution.** Nothing has fired on a real pull request: no comment posted, none updated in place, no baseline committed back. The docket is exercised; the delivery is not. |
| [git-LFS proven as git-LFS](0018-git-lfs-proven-as-git-lfs.md) | The store, producing verdicts identical to the directory and remote backends on the same scenarios. | **The filter, ever running.** No image has been committed through clean/smudge, so an un-smudged checkout handing back a pointer file where a PNG should be has only ever been simulated. |
| [Provenance without React](0019-provenance-without-react.md) | `attributeProvenance` — 25 lines over two `data-*` attributes — and a provenance callback that makes the framework a count of implementations. | **An application.** No Vue, Svelte or Angular tree has been through it, so the claim rests on twenty-five lines and two ecosystem facts. |
| [A cross-browser grid](0020-a-cross-browser-grid.md) | `browser` as a config field, the engine in the identity a baseline is stored under, and all three engines painting one document at identical dimensions. | **The corpus, run twice.** Every `prepare` trick was tuned against Chromium and none declares which engines it was verified on. A selectable engine is not a grid. |
| [Tribunal on a real deployment](0021-tribunal-on-a-real-deployment.md) | The baseline store, history backend, build-and-approve model and review surface, with the real SQL executed through `node:sqlite`. | **The platform.** Never run on Cloudflare, so batch atomicity, quotas, object-size ceilings, concurrent Workers and migrating a database that already has rows are all unmeasured. Identity and metering are [0014](0014-hosted-who-the-caller-is-and-what-the-bill-counts.md), deliberately after this. |
| [Evidence from code this project did not write](0022-evidence-from-code-this-project-did-not-write.md) | One corpus, 40 declared cases, both profiles agreeing on it, and two confrontations in `cases/`. | **A denominator that is not ours.** No third-party component library has been scored, the false-alarm rate has never been measured against the demonstrated false alarm, the `list` arm has never reached a verdict through `variance run`, and three headline figures are asserted by no test. The cross-subject graph joins the list: eight components in one application, and every attribution measured from a declared file list rather than from a `--since` against real history. |
| [What a prop controls](0024-what-a-prop-controls.md) | The measurement: seven props of one design system, each contrasted against a rendering holding everything else still, resolving to five distinct sets of bands — and the finding that an added `OwnerFrame` field reaches no stored digest, so this costs no baseline. | **The name of the prop.** A boundary carries one digest over its whole props object, so a run says *this component was handed something different* and never *which thing*. The join in the spike is a table a person wrote. |
| [`accept` tells a new baseline from a changed one](0023-accept-tells-new-from-changed.md) | The refusal that holds: acceptance promotes an image the run produced and never renders one. | **The distinction.** `--all` promotes a never-reviewed subject and a just-regressed one identically, so the gate becomes a recorder — a constraint currently carried by a paragraph telling operators not to do it. |
| [Component relations](0025-component-relations.md) | The file graph, scanned from source and walked backwards from a diff, with `component` as a node kind and a `declared-in` edge reaching it ([ADR-0038](../context/adr/0038-a-change-reaches-a-component-through-files.md)). | **The edge between two components.** `Card → Button` is not a fact this structure holds, so *where does `Button` appear* and *what does `Card` render* are answered through files — which a barrel file turns into everything reaching everything. Four decisions block it, starting with what counts as *renders* when a component arrives through a prop or a variable. |
| [Selection by closure digest](0026-selection-by-closure-digest.md) | `closureOf` and `driftedBetween`: one digest per node over its whole input closure, cycles condensed, unread inputs marked volatile and propagated ([ADR-0039](../context/adr/0039-the-digest-is-the-proof-the-trail-is-the-explanation.md)). | **The storage and the comparison.** A closure digest is only meaningful against an earlier one, and nothing writes one down — so selection still answers from a diff, and inherits every way a shallow clone, a rebase or a wrong `--since` ref can shape one. |
| [A test is selected by what it executed](0027-a-test-is-selected-by-what-it-executed.md) | The product definition: an instrumented run over source and tests produces coverage data, then a diff joined to that data produces tests to execute. | **The whole capability**, split into [0028](0028-the-instrument.md), [0029](0029-what-a-run-remembers.md) and [0030](0030-a-diff-lands-on-blocks.md). `variance` cannot run a test suite. The static selector answers *37 files import something connected to this*; the number worth reporting is *7 tests executed it, 2 of them directly*, and the half it closes is the over-inclusion a static graph cannot avoid — an import counted as a render on a fork the run never took. The fork that has never gone the other way is observable by nothing, and stays [`selecting.md`](../selecting.md)'s position rather than this one's target. |
| [The instrument](0028-the-instrument.md) | The source transform. `instrument(source, id)` ships at `packages/sense/src/instrument/index.ts` and emits probes and block metadata; it does not run tests or produce coverage data. | **The worker runtime, runner seams, coverage index, diff-to-block mapping, and test selection.** The transform becomes test selection only when an instrumented source-and-test run produces coverage data and a later diff reads that data to choose tests. |
| [What a run remembers](0029-what-a-run-remembers.md) | Nothing. | **The journal, the index, and the identity a block is stored under.** No crossing is recorded, and the rule deciding when a row may be *trusted* rather than merely read does not exist: a test may be excluded only if a complete, current observation of it is held, and a failing test flushes a prefix of its real reach. |
| [A diff lands on blocks](0030-a-diff-lands-on-blocks.md) | Nothing. | **The mapping and the ladder.** Nothing turns changed lines into a set of tests. The diff must *move* the index — shift what only shifted, mint where structure moved, burn inward to contained blocks and outward to enclosing ones — and every rung climbed must be printed, because a run that declined to narrow and a run that found nothing look identical. |
| [A contract is a subject](0031-a-contract-is-a-subject.md) | The material and the untyped floor. `SubjectRef.kind` includes `value`, `CaptureMaterial` carries a `CapturedValue`, and `snapshotValue` writes one from a plain Node test — canonical text, addressed by content, shaped by `drop`, `replace` and `arrayKey`, so a row inserted at the top of a two-thousand-row list is one change. `compareValues` turns two captures into pointer-bearing deltas whose fingerprints group the same edit across rows, across subjects and across runs. | **The baseline, the second join, and the readers.** A value capture reaches `variance run` and is refused by name: the store holds rasters under a render identity, and there is nothing there to compare a text against. `clusterChanges` still reads fingerprints off rectangles only, so a value subject would land in `ungrouped`; and no dialect reader wraps oasdiff or `@graphql-inspector/core`, so their positions, rule names and criticality are not yet in the record. |
| [A render nobody committed](0032-a-render-nobody-committed.md) | The environment key, covering every non-code render input except the application's own state, and the flake pipeline that reads a subject twice ([ADR-0030](../context/adr/0030-two-second-passes-one-variable-each.md)). The *declared* arm is now legible: a subject carrying `variance-parent:<id>` is compared against the subject it varies in the same run, and the difference carries a digest that holds still while both sides move together ([ADR-0045](../context/adr/0045-a-subject-may-be-a-variation-of-another-subject.md)). | **The assignment.** Nothing records which arm a render was produced *under*, so two arms of one flag still share a baseline they disagree about. A subject under a percentage rollout is drawn into an arm per browser context, `again` reads two answers, and the run reports an unstable component and a `file:line` in code nobody edited — every run, at the rollout's own rate. A declared link makes a variation readable; it does not make an undeclared draw recordable. |
| [Two sides a person chose](0033-two-sides-a-person-chose.md) | Everything a comparison is made of: normalized documents, band-exact component hashes, delta banding, component attribution, source resolution — and `deriveVariation`, which compares two subjects of one run without reaching a verdict, through the same `compareTrees` a verdict uses. | **A caller that takes two names.** The cross-subject arithmetic exists and only one thing may ask for it: a subject that declared a parent before the run started. *What is different between `Button` and `IconButton`*, asked afterwards about two subjects that declared nothing, still cannot be typed. Run against run needs one thing more: a run keeps hashes and rasters, and no addressable capture. |
| [A flake is proven by what the run did not execute](0035-a-flake-is-what-the-run-did-not-execute.md) | The halves, on either side of a join that does not exist. The attribution ladder reaches `unexplained`, `again` reads a subject twice, and `held` supplies the control group; the coverage index records which tests entered which regions. | **The join, and the verdict it makes possible.** `unexplained` is an absence — four rungs tried, none held — so it is produced identically by a genuine flake, by an `edited` rung a barrel file hid, and by a run with no change set. A footprint addressed by subject rather than by test file would make the finding a positive observation: the regions this rendering executed do not intersect the change, so nothing in the change explains the movement, on the first run and with no second reading. It needs probes in the browser bundle before a Chromium subject has any evidence at all, and it narrows nothing — the rung classifies a movement the run already found. |
| [A journey crosses processes, and no import declares it](0036-a-journey-crosses-processes.md) | A file graph derived from specifiers, both selection grounds, a merge that survives shards — and the head: one opaque journey id per execution carried on a cookie, an async-context collector inside a Node service, and one record per run joining every head's crossings to the page's against every store the run drove, so two builds of one shared file add up instead of retiring each other. | **The edge, and the hop after it.** `fetch('/api/cart')` is a string, so a component and the handler that answers it have no edge of any kind. A head that can be rebuilt is watched; one that cannot — somebody else's binary, a service the adopter does not build — is as invisible as before, and a backend change there runs the whole suite and lands in `unexplained` when it moves a pixel. Nothing records the call, nothing resolves an endpoint to a file, and nothing forwards the id onto the request a handler makes next, so the second hop is unattributed however well it is instrumented. |
| [A closure per node, at every grain the tree has](0037-a-closure-per-node.md) | `closureOf` over the file graph, `nodeClosures` and `sharedClosures` over the document tree, the own-node component hash with its named holes, the echo at the boundary, and the fingerprint that removes position and value from a difference. | **The same digest over the fiber and the journey, and the grain between own and whole.** The document tree is hashed over everything under a node and joined across subjects; nothing hashes a boundary or a journey region that way, so *this whole `Modal` recurs on three pages* is still a sentence no digest can say, and the three trees' answers cannot be laid beside each other. The middle grain — what a component placed itself, to any depth, with a hole where something was handed in — is the one ADR-0035's rule already decides and nothing applies below the boundary. |
| [A journey is read against the committed tree](0038-a-journey-is-read-against-the-committed-tree.md) | One presence bit per region and subject, the partings among a run's subjects, and the structural component path ADR-0055 gives an update initiator ([ADR-0056](../context/adr/0056-a-journey-is-the-places-visited.md), [ADR-0055](../context/adr/0055-update-initiators-are-structural-attention.md)). | **The join, the key, and the place that moved.** Nothing lays a subject's journey beside the tree React committed for it, so a region entered by a discarded render attempt is the subject's path as much as the render that won; nothing keys a journey by build mode, first read or engine, so a parting across two keys reads as a parting; and a region one reading entered and the next did not, with the inputs held, is not named. Inline JSX handlers are `anon#n` and the join cannot find them. |
| [A subject is found from a description](0039-a-subject-is-found-from-a-description.md) | The lexicon and the structure rows, written by the run beside the census; `variance_locate` over ids and every field with the field printed under each hit; `variance_composition {subject}` printing the rows and the renderings shared, three absences kept apart; the journal read for every region each subject entered ([ADR-0057](../context/adr/0057-the-run-writes-every-name-it-saw.md)). Measured on todomvc: 19 of 20 first, 20 of 20 within three, 5 of 5 refused. | **The suite where the summary does not fit, and four fields.** On fifteen subjects a summary scan scores 20 of 20, so nothing shows the tool earning its place; no generated plan of a few hundred subjects exists to measure it on. No story file and line, no state keys, no parent on a structure row, no co-occurrence. |
| [A baseline in git is a picture and nothing else](0040-a-baseline-in-git-is-a-picture.md) | One pair sourced from two roots — `baselines.records` sends the `.json` where version control is not looking, so an edit that repaints identical pixels writes no tracked file — and both halves optional on their own terms: a record with no image is a subject that occupied no pixels, and `pictured` narrows the three fields that move together. | **The record's own backend, and the rule that makes a cold one survivable.** The second root is chosen by the same code that chose the first, so a record can only ever be a directory; `readRaster` reads a missing sidecar as corruption rather than as a fresh clone; and nothing sends the record to `tribunal`, to an uncommitted local database, or to the CI cache while the image goes to git. A pixel-less baseline would then have nothing in version control at all and promote as new, so a store has to say whether its silence means *no baseline* or *not held* — and `--all` has to tell a new subject from a changed one ([0023](0023-accept-tells-new-from-changed.md)) before this can run on a cache. |
| [Orientation is the first five minutes](0041-orientation-is-the-first-five-minutes.md) | The representation and the reader: every run writes the lexicon, `variance_locate` ranks over it, and the arrangement half prints a place rather than a second id. | **A standard, a size, the noise, and the page that owns it.** No precision, recall or refusal count anybody agreed to before it was measured, and the only figures are from fifteen subjects a summary scan answers without the tool. Nothing says what the index costs at twenty million lines; nothing measures what a start point removes; nothing strips the providers, consumers and HOC-era wrappers a six-hundred-deep tree is mostly made of; nothing prints how many hops a component is from the nearest example that mounts it; and search has no page written for the person who wants to search. |
| [A start point is a path sense holds](0042-a-start-point-is-a-path-sense-holds.md) | The grammar and the exactness: three widths, anchored at the root, whole segments, case included, one failure mode — not found — with every fuzzy rule removed. | **The authority and the closure.** A path is tested against the file paths the report recorded, so most of a real tree answers *not found* and a build-host path the run wrote answers *found*; and a path that resolves narrows to the subjects whose own files matched rather than to everything reachable from it, at any depth, in both directions. Sense is the only thing that may say a file exists and the module graph is what decides scope, and neither is wired in — the server holds a report path, `RunReport` names no tree, and `Tool.run` has no channel to carry one. |
| [A record costs what the run cost](0043-a-record-costs-what-the-run-cost.md) | The read side, end to end: a snapshot is queried where it lies, the crossing relation is interned once per file, and a selection decompresses the runs it touches rather than the file. | **A producer and a publish priced by the run.** Upstream of the encoder a region still holds an array of test-file strings, so the pool's win is bought and thrown away on the way in; a publish rewrites every column and re-interns the whole pool to replace ten modules of 200,000, so the cost is proportional to the file rather than to the change; and there is no append-only overlay, no chosen cadence, and no gate that fails when an operation exceeds the memory a machine was promised. |
| [A contribution is never lost quietly](0044-many-writers-one-record.md) | Three fan-ins that each work alone: worker to run, shard to index, run to index, with a union-within-a-generation rule settled and a lock for one of the three. | **The exclusion, the ordering and the accounting.** The reporters read-modify-write the index with no lock and the names table with none either, so a second project silently discards the first's whole contribution and two paths can be handed one id; the lock that does exist writes a pid it never reads and releases by deleting whatever it finds; no snapshot carries a generation, so no two can be ordered; and every merge computes what it dropped and then throws the number away. |
| [A snapshot states its own age](0045-a-snapshot-states-its-own-age.md) | A columnar binary that refuses a file it cannot read, checks its shapes, and validates values as their columns materialize. | **Self-description.** No magic bytes, no checksum over anything, no persisted model version, and one exact-equality gate that turns every release into a cache eviction — so a foreign file, a file one release old and a file with a flipped bit all arrive as the same untyped error and every caller answers all three by re-recording. Sortedness, the invariant every lookup rests on, is checked nowhere. |
| [The safety rule lives in one place](0046-the-safety-rule-lives-in-one-place.md) | A narrowing that returns all four sets — whole, entered, unread, stale — with a reason for every selection, and a frame check that refuses a row whose text has moved. | **An enforced contract between the record and its answer.** The rule that makes the answer safe is written four times in four dialects across four callers and appears in no shipped line of the library; two exported surfaces hand out `entered` alone; two commands disagree about whether the importer walk runs; a missing commit is reported as every module being stale, with advice that cannot fix it; per-region digests, preconditions' digests and import-time crossings are recorded, folded and never read; and the frame check asks the snapshot's own commit rather than the diff base, which narrows without a refusal. |
| [A skip list is worth what it skips](0047-a-skip-list-is-worth-what-it-skips.md) | The cost side, thoroughly: bytes on disk, peak to build, peak to query, and the experiment that would catch a miss. | **Both numbers that decide adoption.** Nothing has measured how much of a suite the record removes or how often it removes something it should not: the miss-rate instrument has never been run over a mutation plan because none is in the tree, the fixture behind every published figure is not in the repository and the generator that remains has the wrong shape, the suite-size axis is held fixed in the arms every published figure comes from, the one instrument that exists measures a configuration the product does not ship, and the metrics page carries no selection question at all. |
| [A worker that dies says so](0048-a-worker-that-dies-says-so.md) | A framed journal that refuses a truncated file rather than decoding it short, one write per test file carrying every module the worker loaded, and a completeness gate that keeps an unrecorded file out of `whole` so it cannot be skipped. | **Isolation, durability and one answer per question.** The refusal is taken in bulk: one damaged journal, or one `.DS_Store`, throws for the whole run and names no file. The write has no temporary name, no rename and no fsync. An unknown module id is answered three ways across the two reporters — a throw, a silent drop that narrows, and a demotion — and the two runners disagree about resetting between runs, so Vitest watch mode records only the first. Neither settle has a `try`/`finally`, and zero status markers stand at any of it. |
| [Nothing can ask the record where it stands](0049-nothing-can-ask-the-record-where-it-stands.md) | The region-to-tests direction is the record's primary relation and costs a binary search, one module's regions and one pool run per region, at any repository size; `testsReaching` now asks it. | **A surface, a name, and half a distance.** Every shipped entry point takes a diff, so nobody standing on a line can ask who goes there: the served tool that answers it is declared, tested and opened by no binary, `variance ask` cannot reach it, and `variance select` refuses a positional. The answer names test files, because individual test names live only in a JSON sidecar one runner writes when asked. Import hops compose onto the answer and cost a pass over the whole file; call-stack depth is foreclosed and still advertised. |
| [Reconciling a discovered plan with the store](0050-reconciling-a-discovered-plan-with-the-store.md) | The signal. A run asks the baseline store what it holds under this run's identity that the plan never named, and reports every one by name — asked of the whole plan, so a `--since` or `--subjects` run reports its saving as a saving and not as a suite of abandoned baselines ([ADR-0063](../context/adr/0063-a-plan-that-is-discovered-is-half-a-list.md)). | **The verb, the second reading, and two backends.** Nothing acts on the names: there is no command that retires a baseline, so the only way to act on the sentence is `rm`, and the run repeats it forever. A record does not say which suite approved it, so the report has to name both readings — a subject the collector stopped listing, or a second suite sharing the root — and cannot say which. The remote store does not implement the method and `doctor` does not say so, which makes *this store cannot answer* indistinguishable from *nothing is abandoned*. |
| [A sensitivity is scoped by id and nothing else](0051-a-sensitivity-is-scoped-by-id-and-nothing-else.md) | The declaration and its unit: bands rather than thresholds, a reason and a place on every rule, and the count of what each one absorbed printed in the report. | **Every axis but the id.** `subjects` is a list of globs matched against the subject id, so one declaration covers a whole page at one grain and the operator picks the grain that hurts least: a route declared strictly turns red on a token that landed in forty pages, and one declared loosely absorbs a content defect inside the panel it exists to show. A journey knows which regions a subject entered, but nothing joins a region to the nodes it produced, so the finest thing the two sides share may be the component and nobody has measured whether that attribution is stable enough to scope a declaration to. |
| [A run holds every tree it rendered](0052-a-run-holds-every-tree-it-rendered.md) | The rule, stated and obeyed one array over: `retained` is bounded before the loop by the variations that will actually be compared, and says why — a suite of three hundred subjects would otherwise hold three hundred normalized trees to compare four of them. The rasters are already released as they are written. | **The same bound on the trees.** `compositions` keeps every subject's whole tree until the report is composed, and `readings` keeps a second reference whenever history is configured, so peak memory is subjects × tree: 57 MB per subject measured on the 502-route Docusaurus website, about 28.8 GB for the suite, dead at 137 subjects on a default heap. One of its two readers is a pure per-subject fold sitting in the wrong place; the other needs whole subtrees at sites only the cross-subject fold can name, and the list of those sites is computable from the instances alone. |
| [A surface inside a suite reaches less](0053-a-surface-inside-a-suite-reaches-less.md) | The surface-by-feature table on [connect your suite](../surface.md#5-what-each-surface-reaches), and the two halves already closed: evidence images beside a Playwright verdict, and an execution record from every runner seam. | **The run.** A surface that hands subjects to `variance run` inherits sensitivity from configuration, the three images, acceptance and retention; one embedded in the adopter's test process writes no run record and inherits none of them. `vitest-browser` holds both rasters in its judging half and writes no diff; neither embedded surface can answer whether a subject moved before, because `@variance-authority/history` has one importer. Two recording placements are unreached for the same reason and not the same one: a browser-mode test body has no `node:fs` to write a journal with, and a served application executes in a process the route collector never wired a journey head into. |

## Discharged

Numbers are not reused. A gap in the sequence means a spec completed its
lifecycle, not that one was skipped.

| Was | Lifted into |
|---|---|
| A language other than JavaScript | [ADR-0066 — a language is a reader, not a Sense of its own](../context/adr/0066-a-language-is-a-reader-not-a-sense.md) |
| What a diff reaches, on stdout | [ADR-0067 — a run list refuses where a skip list degrades](../context/adr/0067-a-run-list-refuses-where-a-skip-list-degrades.md) |
| Runtime scenarios over named preconditions | [ADR-0047 — a runtime scenario is a witnessed path](../context/adr/0047-a-runtime-scenario-is-a-witnessed-path.md), [ADR-0048 — a scenario archive holds semantic objects](../context/adr/0048-a-scenario-archive-holds-semantic-objects.md), and [`scenarios.md`](../scenarios.md) |
| Cause-first ranking on every path | [ADR-0027 — a baseline carries what its document said](../context/adr/0027-a-baseline-carries-what-its-document-said.md) |
| An ignore is a declaration, not a blind spot | [ADR-0025 — an ignore names a place or a shape](../context/adr/0025-an-ignore-names-a-place-or-a-shape.md), [ADR-0026 — `ignored` is not `unchanged`](../context/adr/0026-ignored-is-not-unchanged.md) |
| Per-component band hashing | [ADR-0018 — a component's hash covers its own nodes](../context/adr/0018-a-component-hash-covers-its-own-nodes.md) |
| Command-line interface | [ADR-0017 — the exit code is the interface](../context/adr/0017-the-exit-code-is-the-interface.md) |
| Artifact storage: git-LFS and remote | [ADR-0016 — where a baseline is kept decides nothing](../context/adr/0016-where-a-baseline-is-kept-decides-nothing.md) |
| CI integration and PR feedback | [ADR-0019 — one comment, updated in place, leading with causes](../context/adr/0019-one-comment-that-leads-with-causes.md) |
| Storybook adapter | [ADR-0020 — read the artifact, not the configuration](../context/adr/0020-read-the-artifact-not-the-configuration.md) |
| Inspection rules, and where they stop | [ADR-0015 — a rule belongs here if a stored snapshot can decide it](../context/adr/0015-a-rule-is-what-a-stored-snapshot-can-decide.md) |
| Self-hosted review backend | [ADR-0021 — approval promotes an image that already exists](../context/adr/0021-approval-promotes-an-image-that-already-exists.md), [ADR-0022 — deciding is not writing](../context/adr/0022-deciding-is-not-writing.md), [ADR-0023 — a service is named for what it is](../context/adr/0023-a-service-is-named-for-what-it-is.md) |
| The first published release | [ADR-0060 — a release is one version across every package, and a person presses it](../context/adr/0060-a-release-is-one-version-and-a-pressed-button.md) |

**Linux verification has no ADR of its own**, and that is the honest outcome: it
forces no decision. It restates ADR-0010's portability claim and ADR-0011's
`incomparable` rule and asks for them to be *measured* on a second machine, which
is a task rather than a decision.
[`docker/linux-verify.sh`](../../docker/linux-verify.sh) is that task, it carries
its own argument, and it has never been run — which the checkpoint tracks, where
unexercised claims belong.

## What a spec here owes

1. **State what is missing, not what exists.** The code is the record of what
   exists, and a spec that describes shipped behaviour is a second copy of it
   that drifts.
2. **Say what would discharge it**, concretely enough that somebody could start.
3. **Leave when the capability lands.** Write the ADRs for the decisions it
   forced, update [`docs/context/checkpoint.md`](../context/checkpoint.md), and
   delete the file. All three, or the debt moves somewhere less visible.

`tools/docs-links.check.ts` and `tools/docs-proposals.check.ts` hold the parts of this that are checkable: every
link resolves, every type a proposal names still exists, and any block listing the
CLI's commands is the binary's own.

## Standing constraints

These hold for anything built here and do not need restating.

- **A published version constrains a change made here.** `@variance-authority/*`
  is on npm under one number for every package, so a rename has adopters and the
  consumer examples are where it is felt first
  ([ADR-0060](../context/adr/0060-a-release-is-one-version-and-a-pressed-button.md)).
  Obtaining this software does not mean cloning it, and nothing a spec proposes
  may assume it does.
- **No telemetry, no analytics, no phone-home.** The only outbound network call
  in the system is to a renderer endpoint the operator supplies.
- **The cheap path requires no infrastructure.** Any capability that needs a
  backend MUST degrade to a working single-run tool without one, and MUST report
  the absence rather than defaulting to a silent negative.
- **Never store pixels in anything that accumulates.** Images are artifacts with
  their own retention (ADR-0011). History is text.
- **An unobservable difference is never reported as no difference** (ADR-0002,
  ADR-0008).
