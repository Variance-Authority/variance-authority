export default {
  parameters: {
    // Deliberately no global decorators. A decorator that stabilises the
    // subject would hide exactly the instabilities these stories exist to
    // present to the adapter.
    layout: 'centered',
  },
};
