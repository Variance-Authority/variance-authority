import { Button } from './Button.jsx';

export default { title: 'Button', component: Button };

export const Neutral = { args: { label: 'Save' } };
export const Primary = { args: { label: 'Save', tone: 'primary' } };
